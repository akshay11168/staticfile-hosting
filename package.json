{
  "name": "staticfile-hosting",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.browser.json",
    "generate-qr": "tsx scripts/generate-qr.ts",
    "create-samples": "tsx scripts/create-sample-pdfs.ts",
    "prepare-assets": "npm run create-samples && npm run generate-qr && npm run build"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "@types/qrcode": "^1.5.5",
    "qrcode": "^1.5.4",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2"
  }
}
